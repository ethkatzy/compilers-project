{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | tr -d '\\r' | { read -r f; case \"$f\" in *.py) poetry run autopep8 --in-place \"$f\" 2>/dev/null;; esac; } || true"
          }
        ]
      }
    ]
  }
}
